ofxCvGrayscaleImage		kinectOutput;
ofxCvGrayscaleImage		kinectOutputCropped;
vector <ofPoint>		quadCropCorners;


quadCropCorners.reserve(4);


quadCropCorners.push_back( ofPoint(50,10 ));
quadCropCorners.push_back( ofPoint(100,10) );
quadCropCorners.push_back( ofPoint(100,100) );
quadCropCorners.push_back( ofPoint(50, 100) );

kinectOutput.setFromPixels(kinect.getDepthPixels(), kinectWidth, kinectHeight);

kinectOutput.allocate(kinectWidth, kinectHeight );
kinectOutputCropped.allocate(kinectWidth,kinectHeight );	


getQuadSubImage(&kinectOutput, &kinectOutputCropped, &quadCropCorners, 1);